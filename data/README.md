# data/

Local, regenerable datasets. Contents are gitignored; only this README and
`.gitkeep` are tracked.

Planned source: the Kaggle Credit Card Fraud dataset. It has PCA features
V1–V28, `Time` and `Amount` only, so card, merchant and device fields need a
synthetic generator or mapping strategy (G-04). Nothing is downloaded yet.
